// Code your solution in this file.
function lowerCaseDrivers (array) {
  return array.map ( function (driver) { driver.toLowerCase();})
};

function nameToAttributes (array) {
  return array.map (function (driver) { return firstName => driver.split(' ')[0], lastName=> driver.split(' ')[1] })
};

function attributesToPhrase (array) {
  return array.map ( function (driver) { return console.log(`${this.name} is from ${this.hometown}`)}
};
